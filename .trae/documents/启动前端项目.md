因后端服务已在运行（Terminal 5），本计划仅需启动前端并验证整体服务。

### 1. 检查后端状态
- 确认 Terminal 5 中的 `api.server` 进程正常（端口 9001）。

### 2. 启动前端（Desktop）
- 新建终端。
- 进入 `desktop/` 目录。
- 执行 `pnpm dev` 启动 Vite + Electron 开发环境。

### 3. 验证
- 确认前端预览地址 `http://localhost:5173/` 可访问。
- 确认后端文档 `http://localhost:9001/docs` 可访问。
